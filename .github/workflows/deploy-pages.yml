name: Deploy Prediction to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'domains/finance/projects/stock-market/development/software/pages/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: domains/finance/projects/stock-market/development/software/pages/package-lock.json

      - name: Install dependencies
        working-directory: domains/finance/projects/stock-market/development/software/pages
        run: npm ci

      - name: Build stock-prediction app
        working-directory: domains/finance/projects/stock-market/development/software/pages
        run: npm run build

      - name: Assemble artifact
        run: |
          mkdir -p artifact/stock-prediction
          # Home page at /prediction/ root
          cp domains/finance/projects/stock-market/development/software/pages/home.html artifact/index.html
          # Stock market app at /prediction/stock-prediction/
          cp -r domains/finance/projects/stock-market/development/software/pages/dist/* artifact/stock-prediction/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: artifact

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
